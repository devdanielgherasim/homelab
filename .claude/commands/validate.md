---
description: Run change-aware validation gates for this repository
---

Invoke the `validate-changes` skill for the currently changed files (or
for `$ARGUMENTS` if given as an explicit file/path list). Report results
using the pass/fail format from that skill — don't just paste raw tool
output.
