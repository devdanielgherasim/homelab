---
description: Create a new Architecture Decision Record
---

Invoke the `create-adr` skill for the decision described in `$ARGUMENTS`.
If no argument is given, ask what decision this ADR should capture rather
than guessing.
